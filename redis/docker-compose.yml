services:
  redis:
    image: redis:latest
    container_name: redis
    restart: no

    ports:
      - "6379:6379"

    volumes:
      - data:/data

    command: redis-server --appendonly yes

volumes:
  data: