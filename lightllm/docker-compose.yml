services:
  litellm:
    image: ghcr.io/berriai/litellm:main-stable

    ports:
      - "127.0.0.1:4000:4000"

    env_file:
      - .env

    volumes:
      - ./config.yaml:/app/config.yaml:ro

    command:
      - "--config=/app/config.yaml"
      - "--port=4000"

    network_mode: "host"